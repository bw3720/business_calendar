# CLAUDE.md

이 파일은 `business_calendar` 프로젝트 작업 시 Claude Code에게 적용되는 지침입니다.

## 변경 이력 관리

이 프로젝트에서 코드 변경(기능 추가, 버그 수정, 리팩토링 등)을 완료할 때마다 `CHANGELOG.md`에 아래 형식으로 간략히 기록할 것.

- 날짜(`## YYYY-MM-DD`) 아래에 무엇이 어떻게 바뀌었는지 한두 줄로 서술
- 같은 날 여러 변경이 있으면 같은 날짜 섹션에 항목을 추가
- 사소한 오타 수정 등 실질적 변경이 아닌 것은 기록하지 않아도 됨
